import styled from "styled-components";

export const CloseChartWrapper = styled.button`
  position: absolute;
  top: 0.5rem;
  left: 0.5rem;
  cursor: pointer;
  border: none;
  z-index: 20;
  background: none;
`;
